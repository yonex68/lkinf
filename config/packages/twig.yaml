twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: ['bootstrap_4_layout.html.twig']
    #form_themes: ['bootstrap_4_Horizontal_layout.html.twig']
    globals:
        appName: 'Links Infinity'
        appEmail: 'contact@links-infinity.com'
        appPhone: '04 65 27 06 06'
        currency: ' €'
        ashtag: '#'
        appConcepteur: "BAYE Conception"
        app_environement: '%env(APP_ENV)%'
        stripe_public_key_test: '%env(STRIPE_PUBLIC_KEY_TEST)%'
        stripe_public_key_live: '%env(STRIPE_PUBLIC_KEY_LIVE)%'
        map_box_token: '%env(MAPBOX_TOKEN)%'
        appSite: 'links-infinity.com'
        appSiteLocal: 'https://127.0.0.1:8000/'

when@test:
    twig:
        strict_variables: true
