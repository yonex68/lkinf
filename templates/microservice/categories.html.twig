{% extends 'base.html.twig' %}

{% block title %}({{ microservices.getTotalItemCount }}) {{ categorie.name }} {% endblock %}

{% block alternatiflink %}https://links-infinity.com/microservices/categories/{{ categorie.name }}{% endblock %}

{% block meta %}
<meta name="author" content="{{ categorie.name }}" />
<meta name="keywords" content="{{ categorie.name }}: trouver plus proche de vous" />
<meta name="description"
   content="{{ categorie.name }}: trouvez dès maintenant votre Le nom de la catégorie plus proche de chez vous pour vous accompagner dans votre projet." />
{% endblock %}

{% block body %}
<!-- Inner Header -->
{% if categorie.image %}
<section class="inner-header categorieSection"
   style="background-image: url({{ vich_uploader_asset(categorie, 'imageFile') | imagine_filter('very_large_size') }})">
   {% else %}
   <section class="py-5 inner-header bg-degrade categorieSection">
      {% endif %}
      <div class="container">
         <div class="row">
            <div class="col-md-9 text-left">
               <h1 class="mt-0 mb-3 text-dark">#{{ categorie.name }} ({{ microservices.getTotalItemCount }})</h1>
               <div class="breadcrumbs">
                  <p class="mb-0 text-dark font-weight-bold" style="font-size: 1.2em;">
                     Pour tous vos travaux vidéo et audio, le site {{ appName }} vous propose
                     des centaines de freelances disponibles et qualifiés, pour <a href="" class="text-blue">Lire
                        plus</a>
                  </p>
                  <p class="d-none">
                     vous accompagner
                     dans votre projet. Spécialiste des services, vous trouverez en ligne
                     tout ce dont vous rêvez : animation de logo, réalisation de vidéo
                  </p>
               </div>
            </div>
         </div>
      </div>
      <div class="caption"></div>
   </section>
   <!-- End Inner Header -->

   {% include "microservice/composants/_page_contenu.html.twig" %}

   {% endblock %}