{% extends 'base.html.twig' %}

{% block title %}({{ microservices.getTotalItemCount }}) {{ categorie.name }} {% endblock %}

{% block body %}
<!-- Inner Header -->
{% if categorie.image %}
<section class="inner-header categorieSection" style="background-image: url({{ vich_uploader_asset(categorie, 'imageFile') | imagine_filter('medium_size') }});">
   {% else %}
   <section class="py-5 inner-header bg-degrade">
      {% endif %}
      <div class="container">
         <div class="row">
            <div class="col-md-9 text-left">
               <h1 class="mt-0 mb-3 text-light">#{{ categorie.name }} ({{ microservices.getTotalItemCount }})</h1>
               <div class="breadcrumbs">
                  <p class="mb-0 text-light lead">
                     Pour tous vos travaux vidéo et audio, le site {{ appName }} vous propose des centaines de
                     freelances
                     disponibles et qualifiés, pour vous accompagner dans votre projet. Spécialiste des services, vous
                     trouverez en ligne tout ce dont vous rêvez : animation de logo, réalisation de vidéo
                  </p>
               </div>
            </div>
         </div>
      </div>
      <div class="caption"></div>
   </section>
   <!-- End Inner Header -->

   {% include "microservice/composants/_page_contenu.html.twig" %}

   {% endblock %}

   {% block javascripts %}
   {{ parent() }}
   <script src="{{ asset('js/favoris.js') }}"></script>
   {% endblock %}