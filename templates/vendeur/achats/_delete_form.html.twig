<form method="post" class="d-inline" action="{{ path('vendeur_achats_delete', {'id': commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-alt"></i> Supprimer</button>
</form>
