{% extends 'base.html.twig' %}

{% block title %}Retrait{% endblock %}

{% block body %}
    <h1>Retrait</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ retrait.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ retrait.montant }}</td>
            </tr>
            <tr>
                <th>ModePaiement</th>
                <td>{{ retrait.modePaiement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vendeur_retraits_index') }}">back to list</a>

    <a href="{{ path('app_vendeur_retraits_edit', {'id': retrait.id}) }}">edit</a>

    {{ include('vendeur_retraits/_delete_form.html.twig') }}
{% endblock %}
