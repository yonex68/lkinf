{% if app.user == user %}

	{% else %}

		<div class="p-4">
			<a href="{{ path('suivre_new', {id: user.id}) }}" class="js-suivis">

				{% if app.user and user.isFollowed(app.user) %}

					<button type="" class="btn-danger btn btn-block">Ne plus suivre</button>

				{% else %}

					<button type="" class="btn-success btn btn-block">Suivre</button>

				{% endif %}

			</a>
		</div>

{% endif %}
