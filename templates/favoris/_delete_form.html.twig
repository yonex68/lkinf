<form method="post" action="{{ path('favoris_delete', {'id': favori.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favori.id) }}">
    <button class="btn btn-dark btn-sm text-danger"><i class="fa fa-trash"></i> Supprimer</button>
</form>
