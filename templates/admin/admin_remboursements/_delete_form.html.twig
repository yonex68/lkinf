<form method="post" class="d-inline" action="{{ path('app_admin_remboursements_delete', {'id': remboursement.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimé cette demande?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remboursement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
