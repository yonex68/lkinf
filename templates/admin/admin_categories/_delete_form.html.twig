<form method="post" class="d-inline" action="{{ path('app_admin_categories_delete', {'id': categorie.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <button class="btn btn-danger"><i class="mdi mdi-trash-can-outline"></i> Supprimer</button>
</form>
