<form method="post"  class="d-inline" action="{{ path('app_admin_users_delete', {'id': user.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce compte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger"><i class="mdi mdi-trash-can-outline"></i> Supprimer le compte</button>
</form>
